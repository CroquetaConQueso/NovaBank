spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-test.sql

eureka:
  client:
    enabled: false

jwt:
  secret: test-auth-server-secret-key-with-at-least-32-chars
  expiration: 86400000
