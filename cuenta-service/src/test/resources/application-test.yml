spring:
  cloud:
    config:
      enabled: false
  r2dbc:
    url: r2dbc:h2:mem:///cuenta_service_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-test.sql

eureka:
  client:
    enabled: false
