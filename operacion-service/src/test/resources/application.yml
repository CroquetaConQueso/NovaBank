spring:
  application:
    name: operacion-service
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-test.sql
  cloud:
    config:
      enabled: false

eureka:
  client:
    enabled: false

novabank:
  kafka-streams:
    operaciones-sospechosas:
      enabled: false
